import 'package:flutter/material.dart';

class CommonGoalCard extends StatelessWidget {
  const CommonGoalCard({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
